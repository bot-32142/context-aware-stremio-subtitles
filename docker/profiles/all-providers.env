# Enables every provider adapter. Providers that require API keys are skipped
# at runtime when their corresponding *_API_KEY env var is empty.
ENABLE_TRANSLATION=false
SUBTITLE_PROVIDERS=opensubtitles-v3,scs,subdl,subsource,subsro,wyzie
